# Azure OpenAI Configuration
# Get these from your Azure Portal: https://portal.azure.com
AZURE_OPENAI_API_KEY = "your_key"
AZURE_OPENAI_ENDPOINT = "http://your-website"

# RapidAPI Configuration
# Get this from: https://rapidapi.com/
# Subscribe to "Indeed Scraper API" (and optionally "LinkedIn Jobs API" for fallback)
# The same RapidAPI key typically works for both APIs if you're subscribed to both
RAPIDAPI_KEY = "your_key"

# RapidAPI Rate Limiting
# Set this to match your RapidAPI subscription tier's requests per minute limit
# Free tier: typically 3 requests/minute
# Basic tier: typically 10-20 requests/minute
# Pro tier: typically 100+ requests/minute
# Check your subscription details in RapidAPI dashboard: https://rapidapi.com/developer/billing
RAPIDAPI_MAX_REQUESTS_PER_MINUTE = 3

# Optional: Separate API Key for LinkedIn Fallback
# Only set this if you need a different API key for the LinkedIn fallback source.
# If not set, the same RAPIDAPI_KEY will be used for both Indeed and LinkedIn.
# LINKEDIN_API_KEY = "your_separate_key_here"

# Job Source Configuration
# Set USE_LINKEDIN_ONLY = true to skip Indeed entirely and use LinkedIn only
# Useful when Indeed quota is exceeded
USE_LINKEDIN_ONLY = false

# Set SKIP_INDEED_IF_QUOTA_EXCEEDED = true to automatically skip Indeed when quota is exceeded
# The app will automatically switch to LinkedIn when Indeed returns 429 errors
SKIP_INDEED_IF_QUOTA_EXCEEDED = true

# Instructions:
# 1. Copy this file to 'secrets.toml' (remove .example)
# 2. Replace the placeholder values with your actual API keys
# 3. Make sure 'secrets.toml' is in your .gitignore to keep keys secure
