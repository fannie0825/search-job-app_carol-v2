# Azure OpenAI Configuration
# Get these from your Azure Portal: https://portal.azure.com
AZURE_OPENAI_API_KEY = "your_key"
AZURE_OPENAI_ENDPOINT = "http://your-website"

# RapidAPI Configuration
# Get this from: https://rapidapi.com/
# Subscribe to "Indeed Scraper API"
RAPIDAPI_KEY = "your_key"

# Optional: Fallback Job Source API Key
# If you want to use a different API key for the fallback job source (LinkedIn),
# uncomment and set one of these. Otherwise, the same RAPIDAPI_KEY will be used.
# Note: Some APIs require separate subscriptions, so you may need a different key.
# LINKEDIN_API_KEY = "your_fallback_key"
# OR
# RAPIDAPI_KEY_FALLBACK = "your_fallback_key"

# Instructions:
# 1. Copy this file to 'secrets.toml' (remove .example)
# 2. Replace the placeholder values with your actual API keys
# 3. Make sure 'secrets.toml' is in your .gitignore to keep keys secure
