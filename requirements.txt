# Core dependencies with pinned versions for Streamlit Cloud stability
streamlit>=1.28.0,<2.0.0
requests>=2.31.0,<3.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

# Document processing
PyPDF2>=3.0.0,<4.0.0
python-docx>=1.0.0,<2.0.0

# Vector store - use lightweight version for Streamlit Cloud
# Note: chromadb can be memory-intensive; using specific version for stability
chromadb>=0.4.0,<0.5.0

# Tokenization
tiktoken>=0.5.0,<1.0.0

# PDF generation
reportlab>=4.0.0,<5.0.0

# Protobuf version constraint (needed for some ML dependencies)
protobuf>=4.0.0,<5.0.0
